<%= form_with(model: employee, local: true) do |form| %>
  <% if employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :emp_id, 'ID#', class: "col-md-2 control-label" %>
    <%= form.text_field :emp_id, id: :employee_emp_id %>
  </div>

  <div class="form-group">
    <%= form.label :emp_fname, 'Employee First Name', class: "col-md-2 control-label" %>
    <%= form.text_field :emp_fname, id: :employee_emp_fname %>
  </div>

  <div class="form-group">
    <%= form.label :emp_lname, 'Last Name', class: "col-md-2 control-label" %>
    <%= form.text_field :emp_lname, id: :employee_emp_lname %>
  </div>

  <div class="form-group">
    <%= form.label :emp_wage, 'Wage', class: "col-md-2 control-label" %>
    <%= form.text_field :emp_wage, id: :employee_emp_wage %>
  </div>

  <div class="actions">
    <%= form.submit 'Submit', class: "btn btn-primary col-md-2" %>
  </div>
<% end %>
